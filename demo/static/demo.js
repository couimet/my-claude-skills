/**
 * Demo site interactive features (~5KB budget).
 *
 * Features (implemented in later steps):
 * - Intersection Observer: update URL hash as user scrolls past exchange cards
 * - Artifact toggle: expand/collapse artifact panels with smooth animation
 * - Diff toggle: switch between diff and full-content views
 * - Progress bar sync: highlight current phase in sticky header
 * - Keyboard navigation: j/k to jump between exchanges
 * - Hash navigation: scroll to #exchange-N on page load
 */

(function () {
  'use strict';

  // Placeholder — features added in S004 and S005.
})();
