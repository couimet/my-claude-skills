# Local linter versions, pinned to match the shared actions' internal pins
# (couimet/github-actions markdownlint 0.22.1, prettier 3.8.4). Bump here when
# the shared actions advance, so `make lint` mirrors what CI enforces.
MARKDOWNLINT_VERSION := 0.22.1
PRETTIER_VERSION := 3.8.4
