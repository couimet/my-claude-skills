# Tool versions, pinned to match the shared actions (couimet/github-actions).
# Bump here when the shared actions advance, so local runs mirror CI.
MARKDOWNLINT_VERSION := 0.22.1
PRETTIER_VERSION := 3.8.4
SHELLCHECK_VERSION := 0.11.0
